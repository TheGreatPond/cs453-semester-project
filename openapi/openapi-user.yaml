openapi: 3.0.3
info:
  title: CS453 Semeter Project API
  version: 1.0.0
  description: First Swagger doc, describing basic CRUD operations on a list of tasks

servers:
  - url: http://localhost:3000

paths:
  /api/users:
    get:
      summary: Get list of users
      responses:
        "200":
          description: List of users returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
    post:
      summary: "Adds new user to list"
      requestBody:
        description: "needs a username"
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserInput"
      responses:
        "201":
          description: "Null response"
        "400":
          description: "Invalid request, task not created"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorInvalidBody"

  /api/users/{name}:
    get:
      summary: Get user from list
      parameters:
        - in: path
          name: name
          schema:
            type: string
          required: true
          description: name of the user to get
      responses:
        "200":
          description: Single user returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "404":
          description: "Resource requested not found"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotFound"
    # put:
    #   summary: Update one user in list
    #   parameters:
    #     - in: path
    #       name: name
    #       schema:
    #         type: string
    #       required: true
    #       description: name of the user to update
    #   requestBody:
    #     description: "needs name of user"
    #     required: true
    #     content:
    #       application/json:
    #         schema:
    #           $ref: "#/components/schemas/UserInput"
    #   responses:
    #     "200":
    #       description: Single user returned
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/User"
    #     "404":
    #       description: "Resource requested not found and could not be altered"
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/ErrorNotFound"
    #     "400":
    #       description: "Invalid request, User not updated"
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/ErrorInvalidBody"
    # patch:
    #   summary: Update part of one user in list
    #   parameters:
    #     - in: path
    #       name: name
    #       schema:
    #         type: string
    #       required: true
    #       description: name of the user to update
    #   requestBody:
    #     description: "needs a combination of any of the following: name"
    #     required: true
    #     content:
    #       application/json:
    #         schema:
    #           $ref: "#/components/schemas/UserInput"

    #   responses:
    #     "200":
    #       description: Single User returned
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/User"
    #     "404":
    #       description: "Resource requested not found and could not be altered"
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/ErrorNotFound"
    #     "400":
    #       description: "Invalid request, User not updated"
    #       content:
    #         application/json:
    #           schema:
    #             $ref: "#/components/schemas/ErrorInvalidBody"
    delete:
      summary: Delete User from db
      parameters:
        - in: path
          name: name
          schema:
            type: string
          required: true
          description: name of the User to delete
      responses:
        "204":
          description: "Null response"
        "404":
          description: "Resource requested not found and therefore not deleted"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotFound"

  #
  # Remember:
  # - Express routes use :id, such as /tasks/:id.
  # - OpenAPI paths use {id}, such as /tasks/{id}.
  #
  # You may define schemas under components/schemas if you want to avoid
  # repeating the same object structure many times.

components:
  schemas:
    User:
      type: object
      required:
        - name
        - created_at
        - updated_at
      properties:
        name:
          type: string
          example: "exampleuser"
        created_at:
          type: string
          example: "creation_time"
        updated_at:
          type: string
          example: "updated_time"
    UserInput:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: "exampleuser"
  # DONE: Define an ErrorResponse schema with an error message.
    ErrorNotFound:
      type: object
      properties:
        Error:
          type: string
          example: "The resource you were looking for could not be found, please correct your path and try again"
    ErrorInvalidBody:
      type: object
      properties:
        Error:
          type: string
          example: "Invalid request body, please adjust your request and try again."